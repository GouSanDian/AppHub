{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "admin",
  "description": "管理员权限配置（进程监控需要）",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-spawn",
    "shell:allow-execute"
  ]
}
